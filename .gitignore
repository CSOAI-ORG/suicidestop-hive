meok_one/.pytest_cache/

# generated benchmark/test data (regenerable, not source)
data/bft_battery*.json
data/full_suite*.json

# MEOK LAW runtime registry (per-deployment; the law_bundle.json IS the committed source)
meok_one/data/law_agents.jsonl

# openclawworld reference clones (local only — get-the-code)
reference/
